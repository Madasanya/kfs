#ifndef _SCREEN_SETTINGS_H_
#define _SCREEN_SETTINGS_H_

/** @brief Screen width in characters (standard VGA text mode). */
#define SCREEN_WIDTH 80u

/** @brief Screen height in rows (standard VGA text mode). */
#define SCREEN_HEIGHT 25u

/** @brief Maximum length of screen header string, including null terminator. */
#define SCREEN_HEADER_LEN (SCREEN_WIDTH)

#endif /* _SCREEN_SETTINGS_H_ */